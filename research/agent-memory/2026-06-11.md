# 🧠 Agent Memory & Context Research Daily — 2026-06-11

> 搜索批次：A（agent memory episodic memory retrieval augmented generation long-term memory）、B（LLM memory external memory knowledge base agent experience replay）、C（context window context management in-context learning long context agent context compression）
> 筛选自 ArXiv 最新提交（截至 2026-06-10 UTC）

---

## 📄 精选论文

---

📄 **多轮对话中的上下文驱动增量压缩**（Context-Driven Incremental Compression for Multi-Turn Dialogue Generation）
🔗 https://arxiv.org/abs/2606.12411
💡 核心贡献（2句）：提出了一种增量式对话上下文压缩框架，能够在多轮对话中跨轮次共享和修订压缩后的记忆表示，避免了朴素截断和摘要方法导致的信息损失与误差累积。该方法将对话历史的"工作记忆"抽象为可更新的压缩状态，显著降低了长对话中的冗余注意力开销。
🔧 技术亮点：
- 跨轮次记忆共享（cross-turn memory sharing）：每轮压缩结果可被后续轮次修订复用，而非独立处理
- 增量压缩策略：只对新增对话片段进行差量压缩，避免全量重编码
- 可修订记忆机制：支持对已压缩状态的回溯修正，纠正长对话中的累积错误
📊 实验结果：在长对话生成基准上，相比基线方法显著降低了编码开销，同时保持了对话一致性和信息保真度。
🧩 技术分类：
- Memory维度：工作记忆 / 记忆检索
- Context维度：上下文压缩 / 上下文管理
🔗 Memory ↔ Context 关联：通过将压缩后的对话历史作为可修订的工作记忆状态，在上下文窗口约束下实现了长期记忆的有效维护。

---

📄 **Doc-to-Atom：学习编译与组合记忆原子**（Doc-to-Atom: Learning to Compile and Compose Memory Atoms）
🔗 https://arxiv.org/abs/2606.12400
💡 核心贡献（2句）：在 Doc-to-LoRA 的基础上，提出将文档上下文蒸馏为离散"记忆原子"（Memory Atoms）的方法，每个原子对应文档中的一个语义单元，可在推理时按需组合拼装。通过将上下文压缩转化为结构化的记忆编译过程，解决了长文档推理中注意力二次复杂度带来的效率瓶颈。
🔧 技术亮点：
- 记忆原子化（Memory Atomization）：将长文档切分并蒸馏为独立、可复用的语义原子单元
- 单次前向传递编译（Single Forward Pass Compilation）：继承 Doc-to-LoRA 思路，一次性生成所有记忆原子
- 按需组合推理（Compose-on-Demand Inference）：在推理阶段动态组合相关原子，无需重载全量上下文
📊 实验结果：在文档理解和多步推理任务上，推理内存占用和延迟均显著优于全上下文方法，同时保持了相当的准确性。
🧩 技术分类：
- Memory维度：语义记忆 / 外部存储 / 记忆检索
- Context维度：上下文压缩 / 上下文管理
🔗 Memory ↔ Context 关联：将传统的"大上下文窗口"问题转化为"结构化外部记忆编译+检索"范式，是 Memory 与 Context 协同设计的典范工作。

---

📄 **可恢复视觉 Token 路由：不删除，只改道**（Reroute, Don't Remove: Recoverable Visual Token Routing for Vision-Language Models）
🔗 https://arxiv.org/abs/2606.12412
💡 核心贡献（2句）：针对视觉语言模型（VLM）中视觉 token 数量爆炸导致的 KV-cache 内存压力和注意力计算开销，提出"可恢复路由"（Recoverable Routing）机制，以软路由替代硬删除，保留低分 token 的恢复通路。这一设计规避了传统 rank-and-remove 方法因不可逆剪枝导致的信息丢失问题。
🔧 技术亮点：
- 软路由机制（Soft Routing）：低优先级 token 被路由至压缩路径而非直接丢弃，保留信息可恢复性
- KV-cache 内存优化：通过稀疏路由减少进入解码器的 token 数量，降低推理内存峰值
- 训练可微路由：路由决策端到端可训练，避免了硬阈值剪枝的梯度断裂问题
📊 实验结果：在多个 VQA 和图像描述基准上，以更低内存占用实现了接近全 token 保留的性能，优于现有 token 压缩方法。
🧩 技术分类：
- Memory维度：工作记忆（KV-cache管理）
- Context维度：上下文压缩 / 上下文管理

---

📄 **TAHOE：基于经验的 Text-to-SQL 自动化提示优化**（TAHOE: Text-to-SQL with Automated Hint Optimization from Experience）
🔗 https://arxiv.org/abs/2606.12387
💡 核心贡献（2句）：提出 TAHOE 系统，将 Text-to-SQL 中的提示优化抽象为"从历史经验中学习"的问题，通过自动化挖掘过往查询的成功与失败案例来生成和优化 SQL 生成提示（Hint）。该系统无需昂贵的监督微调，通过经验回放式的提示演化，适应不同 SQL 方言和动态变化的 schema。
🔧 技术亮点：
- 经验驱动提示优化（Experience-Driven Hint Optimization）：从历史查询经验中自动归纳提示规则
- 在线适应（Online Adaptation）：随着新查询积累，系统持续更新记忆中的优化提示库
- 无监督微调部署：避免了 RL/SFT 的高成本，依赖轻量级经验记忆即可提升生产环境性能
📊 实验结果：在多个真实数据库 Text-to-SQL 基准上超越了基于 LLM agent 的基线方法，且推理成本显著更低。
🧩 技术分类：
- Memory维度：情景记忆 / 外部存储（经验库）
- Context维度：In-Context学习 / 上下文管理
🔗 Memory ↔ Context 关联：通过外部经验记忆库动态检索最优 Hint 注入上下文，实现了记忆驱动的 In-Context Learning 范式。

---

📄 **DIRECT：具身规划器中何时何处分配测试时计算？**（DIRECT: When and Where Should You Allocate Test-Time Compute in Embodied Planners?）
🔗 https://arxiv.org/abs/2606.12402
💡 核心贡献（2句）：发现在具身 Agent 中盲目扩展测试时计算（test-time compute）会带来高延迟、高 token 消耗但收益递减的问题，提出 DIRECT 框架，智能地判断"何时"和"何处"值得分配更多计算资源。该工作本质上是对 Agent 上下文预算的动态管理问题的系统性研究。
🔧 技术亮点：
- 上下文计算预算感知（Context Budget-Aware Planning）：动态评估当前规划步骤的信息复杂度，决定是否扩展推理
- 步骤级细粒度计算分配：在关键决策节点集中计算资源，避免全局均匀消耗
- 具身场景评估框架：在多个 embodied planning 任务中量化了计算-性能权衡曲线
📊 实验结果：在具身规划任务上，DIRECT 在保持成功率的同时将总 token 消耗降低了约 40%，延迟减少显著。
🧩 技术分类：
- Memory维度：工作记忆（推理上下文管理）
- Context维度：上下文管理 / 上下文压缩
🔗 Memory ↔ Context 关联：通过动态调控写入工作记忆的推理深度，在有限上下文预算下最大化 Agent 的规划质量。

---

📄 **APPO：智能体过程策略优化**（APPO: Agentic Procedural Policy Optimization）
🔗 https://arxiv.org/abs/2606.12384
💡 核心贡献（2句）：针对现有 Agentic RL 方法在工具调用边界或固定工作流等粗粒度单元上分配信用的局限，提出 APPO，实现细粒度的过程级（procedural）信用分配，识别哪些中间决策步骤真正影响了最终结果。通过更精细的经验回放和信用归因，显著提升了多轮工具使用 Agent 的学习效率。
🔧 技术亮点：
- 过程级信用分配（Procedural Credit Assignment）：超越工具调用边界，对每个中间推理步骤赋予差异化奖励
- 细粒度经验回放（Fine-Grained Experience Replay）：在 RL 训练中记录并复用过程级轨迹片段
- 多轮工具使用优化：专为 LLM Agent 的多轮工具调用场景设计，覆盖复杂任务的长轨迹优化
📊 实验结果：在多轮工具使用基准上，APPO 相比粗粒度信用分配基线取得了显著提升，在复杂任务上表现尤为突出。
🧩 技术分类：
- Memory维度：情景记忆 / 外部存储（轨迹记忆）
- Context维度：上下文管理（多轮轨迹上下文）
🔗 Memory ↔ Context 关联：通过将历史轨迹片段作为可检索的情景记忆，在强化学习训练中实现了过程级的上下文感知信用分配。

---

## 🧠 今日洞察

1. **记忆原子化与上下文编译正在成为主流范式**：从 Doc-to-Atom 到多轮对话增量压缩，研究者们不约而同地将"大上下文窗口"问题转化为"结构化外部记忆+按需检索组合"的方案，标志着 Memory-as-Context 设计思路的成熟。

2. **Agent 上下文预算管理进入精细化阶段**：DIRECT 和 APPO 分别从推理计算分配和强化学习信用归因两个维度，探索了如何在有限的上下文窗口和计算预算内最大化 Agent 决策质量——这预示着"上下文感知的自适应推理"将成为近期热点。

3. **Memory 与 Context 的协同设计趋势明显**：经验驱动（TAHOE）、可修订工作记忆（Context-Driven Compression）、可恢复 Token 路由（Reroute）等工作共同揭示了一个核心洞见：记忆不应是上下文的简单副产品，而应主动参与上下文的构建、压缩与动态调度，Memory ↔ Context 的双向反馈机制正在成为新的研究前沿。

---

*📦 自动生成 by Agent Memory 专题日报 Cron Job | 2026-06-11 10:02 CST*
